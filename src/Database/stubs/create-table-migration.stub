<?php declare(strict_types=1);

namespace {{ namespace }};

use {{ foundation_database_migration }};
use {{ foundation_database_schema }};
use {{ foundation_database_blueprint }};
use {{ table_namespace }}\{{ table_class }};

final readonly class {{ class }} implements Migration {

	public const string ID = {{ id_php }};

	public function __construct(
		private {{ table_class }} $table
	) {
	}

	public function id(): string {
		return self::ID;
	}

	public function up( Schema $schema ): void {
		$blueprint = Blueprint::for( $this->table );

		$blueprint->bigIncrements( 'id' );

		// Define the complete initial schema before running this migration.

		$schema->create( $blueprint );
	}

	public function down( Schema $schema ): void {
		$schema->drop( $this->table );
	}

}
